# Optional. Leave unset to use localStorage (default, zero setup).
# Set both to switch the board to a real Supabase backend automatically.
# See README.md > "Optional: real backend with Supabase" for setup steps.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
